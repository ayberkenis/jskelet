<section class="wrapper">
  <h1 class="text-3xl font-bold"><%= metadata.title %></h1>
  <p class="mt-4 max-w-prose">
    Bu sayfa 300 saniye önbellekte kalır. Yanıttaki
    <code>X-JSkelet-Cache</code> başlığı <code>MISS</code> → <code>HIT</code> →
    süre dolduğunda <code>STALE</code> değerlerini alır.
  </p>
</section>
